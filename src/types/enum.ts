export enum ResultEnum {
    SUCCESS = 0,
    ERROR = -1,
    TIMEOUT = 401,
}

export enum BasicStatus {
    DISABLE = 0,
    ENABLE = 1,
}

export enum PermissionType {
    CATALOGUE = 0,
    MENU = 1,
    BUTTON = 2,
}
